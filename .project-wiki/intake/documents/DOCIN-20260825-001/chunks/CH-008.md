---
id: "DOCIN-20260825-001-CH-008"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 008"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-008

- Parent intake: `DOCIN-20260825-001`
- Sequence: 8
- Heading: 目录权限与递归遍历
- Page range: null
- Word count: 350
- Hints: `data-or-integration`

## Text

name; if (handle.kind === "directory") { nodes.push({ id: path, name, path, kind: "directory", handle, // 可改成 lazy loading；此处为了演示递归。 children: await walkDirectory(handle, path), }); } else { // 注意：这里若立刻 getFile() 会产生大量 metadata I/O。 nodes.push({ id: path, name, path, kind: "file", handle, }); } } return nodes; } ``` 大目录不要第一次就对 100,000 个节点逐个 `getFile()` 读取 size。 建议： ```text 第一阶段： handle + name + path 第二阶段： 可见节点/后台低优先级读取 metadata 点击压缩： 才 getFile() + stream() ``` 兼容模式： ```html <input id="directory-picker" type="file" webkitdirectory multiple /> ``` 然后从： ```ts file.webkitRelativePath ``` 重建树。MDN 明确说明 `webkitdirectory` 返回整个目录层级中的文件，并通过 `webkitRelativePath` 保留相对路径。citeturn6search0 ### 三态父子级勾选 数据模型不需要给每个父节点手动存 `mixed`，最好从 children 推导： ```ts type Selection = Map<string, boolean>; function getState( node: TreeNode, selected: Selection, ): "checked" | "unchecked" | "mixed" { if (node.kind === "file") { return selected.get(node.id) !== false ? "checked" : "unchecked"; } const states = (node.children ?? []) .map((child) => getState(child, selected)); if (states.every((s) => s === "checked")) { return "checked"; } if (states.every((s) => s === "unchecked")) { return "unchecked"; } return "mixed"; } ``` 父目录点击后递归： ```ts function setSubtree( node: TreeNode, value: boolean, selected: Selection, ): void { selected.set(node.id, value); for (const child of node.children ?? []) { setSubtree(child, value, selected); } } ``` UI checkbox： ```tsx <input type="checkbox" checked={state === "checked"} ref={(el) => { if (el) { el.indeterminate = state === "mixed"; } }} onChange={(event) => { updateSubtree(node, event.currentTarget.checked); }} /> ``` 对于几万/几十万节点，React 不应该真的 render 几万个 DOM row；需要**树虚拟化**，只 render viewport 附近展开节点。 ### 默认排除规则 建议初始规则： ```ts const DEFAULT_EXCLUDES = new Set([ ".git", ".svn", "node_modules", ".next", ".nuxt", "dist", "build", "target", ".idea", "__pycache__", ".pytest_cache", ".mypy_cache", "venv", ".venv", ]); ``` 但 UX 上一定写： ```text “建议排除” ``` 而不是： ```text “禁止压缩” ``` 用户应能重新勾上。 还可以识别： ```text *.log *.tmp .DS_Store Thumbs.db ``` 不过文件 glob 默认排除应更保守，避免误删用户真正想打包的内容。 ### zip.js + WritableStream + OPFS 核心生产路径大致应写成： ```ts import { ZipWriter, createOPFSTempStream, } from "@zip.js/zip.js"; type SelectedFile = { path: string; handle: FileSystemFileHandle; }; export async function createZip( files: SelectedFile[], saveHandle: FileSystemFileHandle, signal: AbortSignal, onProgress: (done: number, total: number) => void, ): Promise<void> { const output = await saveHandle.createWritable(); let totalDone = 0; const totalBytes = await calculateTotalSize(files); const writer = new ZipWriter(output, { useWebWorkers: true, // 多 entry 并行时避免默认内存临时缓冲。 createTempStream: createOPFSTempStream, }); try {
