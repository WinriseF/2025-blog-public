---
id: "DOCIN-20260825-001-CH-010"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 010"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-010

- Parent intake: `DOCIN-20260825-001`
- Sequence: 10
- Heading: 取消、错误和“恢复”
- Page range: null
- Word count: 350
- Hints: `actor-mentioned`, `security-or-compliance`

## Text

以后若确实要做“跨刷新续写 ZIP”，再针对 zip.js 当前 append/offset 能力单独设计 archive journal，而不要和第一版低 RAM 流式写混在一起。 ### 进度展示 建议同时展示： ```text 正在压缩 backend/src/service/order.ts ████████████████░░░░ 78% 已处理 18.4 GB / 23.5 GB 文件 12,842 / 18,302 当前速度 164 MB/s 已用时间 01:48 预计剩余 00:31 [取消] ``` 速度用滑动窗口，不要： ```ts bytes / 当前 100ms ``` 否则 UI 会疯狂跳。 例如： ```ts const samples: Array<{ t: number; bytes: number }> = []; function updateSpeed(totalBytes: number) { const now = performance.now(); samples.push({ t: now, bytes: totalBytes, }); while ( samples.length > 1 && now - samples[0].t > 5000 ) { samples.shift(); } const first = samples[0]; const last = samples.at(-1)!; const seconds = (last.t - first.t) / 1000; return seconds > 0 ? (last.bytes - first.bytes) / seconds : 0; } ``` zip.js 自身已经提供 entry 级别的 `onstart / onprogress / onend` 回调，进度参数以字节表示。citeturn14search1turn14search2 ## 安全、隐私、UX 与最终建议 ### “完全本地”应该从架构上保证，而不是只写一句宣传语 正确架构： ```text 本地目录 ↓ FileSystemHandle / File ↓ 浏览器本地 JS + Worker ↓ zip.js ↓ OPFS 临时数据（必要时） ↓ 用户指定的 ZIP ``` 业务服务器根本不在这条数据路径里。 不过要真正做到“文件不上传”，还应避免： ```text 把文件名发送给 analytics 把完整目录路径写到错误日志 异常监控自动序列化 File metadata 把文件内容交给第三方 Worker/CDN 动态加载未经固定版本的压缩脚本 ``` zip.js 本身没有第三方 runtime dependency，这是这种本地隐私工具的优势之一。citeturn7search1 建议 production bundle 全部 self-host： ```text index.html app.js zip.js worker zip.js wasm service-worker.js ``` 而不是压缩时突然去 CDN 拉一个 worker。 ### 权限 UX 浏览器不允许网页静默扫描用户 C 盘，这正是安全边界，而不是缺陷。`showDirectoryPicker()` 需要用户操作；后续 write/read permission 也可能是 granted、prompt 或 denied。citeturn2search3turn12search2turn12search4 建议第一次进入页面显示： ```text 文件不会上传 选择文件夹后，本工具只在你的浏览器中读取你授权的内容。 压缩结果会直接保存到你的电脑。 [选择文件夹] ``` 而不是： ```text 请授予磁盘权限 ``` 后者很容易让人误以为网页获得整块磁盘权限。 ### OPFS 也要解释清楚 OPFS 是 origin 私有存储，不是你的服务器；但它仍受浏览器 quota 和站点数据生命周期管理。清除站点数据可能删除这些临时内容。citeturn2search2 因此压缩结束、取消和失败时，都应主动 dispose 临时对象。 UI 可以写： ```text 并行压缩可能临时占用浏览器本地存储。 任务结束后临时数据会自动清理。 ``` ### 大文件前置检查 点击“开始压缩”前最好先算： ```text 已选择文件数 输入逻辑大小 可能压缩的类型分布 OPFS 是否可用 原生直接保存是否可用 ``` 然后动态决定能力： ```text 输出 < 512 MB → 所有 fallback 都可开放 输出 512 MB ~ 4 GB → 建议流式保存 输出 > 4 GB → 强制 Zip64-capable 核心 → 不允许 fflate standalone → 不推荐 Blob fallback 输出几十 GB → 强制真正 WritableStream → 并行必须 OPFS 或降低为串行 ``` 这里 512 MB 不是浏览器规范中的固定限制，而应作为产品自行配置的安全阈值；不同浏览器、机器和移动设备的 Blob/RAM 上限差异很大。 ### 压缩模式可以比 Bandizip 更聪明 建议三档： ```text 智能压缩 推荐 根据文件类型决定 DEFLATE / STORE 最大压缩 更多 CPU，更慢 极速打包 不压缩，只打 ZIP ``` 对： ```text jpg jpeg png webp mp4 mov mkv mp3 aac zip 7z rar apk pdf（部分） ``` 继续 DEFLATE 往往收益有限，却消耗大量 CPU，因此可以默认 STORE，并允许用户覆盖。 这里 `client-zip` 可作为极速打包路径，因为它明确是不压缩的 streaming Zip64 generator；或者统一使用 zip.js 的 STORE compression method，避免引入第二套 ZIP writer。citeturn7search0turn7search10 从长期维护性看，我更偏向后者： ```text 一个
