---
id: "DOCIN-20260825-001-CH-005"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 005"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-005

- Parent intake: `DOCIN-20260825-001`
- Sequence: 5
- Heading: 实际 hands-on 测试结果
- Page range: null
- Word count: 350
- Hints: `actor-mentioned`, `data-or-integration`

## Text

2 GB / 50 GB ``` **没有获得可用数值。** 当前实际状态是： | 浏览器 | 启动 | 本地 Demo | File System picker 实测 | ZIP 项目 benchmark | |---|---:|---:|---:|---:| | Chromium 144 | ✓ | ❌ localhost 被策略拦截 | ❌ 无法进入安全交互页面 | ❌ | | Edge | ❌ 未安装 | — | — | — | | Firefox | ❌ 未安装 | — | — | — | 因此以下四类数字本报告刻意不伪造： ```text Peak RSS 平均 CPU % 总耗时 MB/s ``` ### 浏览器 API 兼容策略 从架构角度，比“zip.js 支不支持 Firefox”更重要的是区分： ```text ZIP 核心能力 ≠ 本地目录/保存 Picker 能力 ``` zip.js 核心本身明确支持当前 Chrome、Firefox、Safari、Edge；不同 build 的最低版本不同。citeturn10search1 但 `showDirectoryPicker()` 仍被 MDN 标记为 Limited availability，并要求 HTTPS、安全上下文和用户主动操作。因此 Firefox 能运行 zip.js，并不等于 Firefox 一定能走与 Chromium 相同的本地目录 handle 流程。citeturn2search3 兼容矩阵建议因此按“能力分层”而不是浏览器名硬编码： | 能力 | Chrome / Edge 新版 | Firefox 新版 | 降级策略 | |---|---|---|---| | Web Streams | ✓ | ✓ | 无需特殊处理 | | `Blob/File.stream()` | ✓ | ✓ | 现代浏览器普遍可用 | | `webkitdirectory` | ✓ | ✓ 新版 | 普通多文件 input | | `showDirectoryPicker` | **优先使用** | 不作为必需前提 | `webkitdirectory` | | `showSaveFilePicker` / 原生直接写 | **优先使用** | feature detect | StreamSaver / Blob | | OPFS | ✓ | ✓ | 无则降低并发 | | Web Worker | ✓ | ✓ | 主线程低并发 fallback | | zip.js | ✓ | ✓ | native build / embedded codec | `Blob.stream()` 自 2021 年起就是广泛可用的基础能力，并且在 Worker 中也可用。citeturn6search2 `webkitdirectory` 目前已经达到 Baseline 2025，但仍应保留 feature detection，因为旧浏览器和旧 WebView 不能假定支持。citeturn6search0 `createWritable()` 返回的是 `FileSystemWritableFileStream`，写入过程中浏览器可使用临时文件，最终 close 后再提交到用户目标文件，这正适合“大输出不能先放 RAM”的场景。citeturn2search0turn2search7 ### 真正应该怎么跑 50 MB / 2 GB / 50 GB 建议测试四条实现，而不是只测试库： ```text A zip.js → BlobWriter B zip.js → createWritable C zip.js + 4 workers + OPFS temp → createWritable D client-zip STORE → createWritable/StreamSaver ``` 再可选： ```text E fflate streaming → WritableStream ``` 这样才能回答真正有意义的问题： > 是压缩算法吃内存，还是最后 Blob 吃内存？是并行 temp 吃内存，还是 Worker 本身吃内存？ 测试数据应固定： | 场景 | 建议数据 | |---|---| | Small | 1,000 文件，共约 50 MiB | | Medium | 4,096 文件，共约 2 GiB | | Large simulated | 50,000+ entries，总逻辑输入约 50 GiB | | 混合类型 | 文本/JSON + PNG/JPEG + MP4/ZIP，避免只测试可高度压缩的全零数据 | 50 GB “模拟”最好使用可重复的 streaming generator，而不是预先把 50 GB 全写在测试 SSD 上；否则磁盘生成测试集本身就会污染 cache 和 I/O 结果。 性能采样建议同时测： ```text wall time logical input bytes / wall
