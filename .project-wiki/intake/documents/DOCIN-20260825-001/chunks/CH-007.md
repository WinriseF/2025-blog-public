---
id: "DOCIN-20260825-001-CH-007"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 007"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-007

- Parent intake: `DOCIN-20260825-001`
- Sequence: 7
- Heading: Tree UI 应该成为产品核心能力
- Page range: null
- Word count: 350
- Hints: `actor-mentioned`, `data-or-integration`

## Text

MB │ ├─ ☑ src │ ├─ ☐ target 298 MB │ └─ ☑ pom.xml │ ├─ ☐ .git 174 MB └─ ☑ README.md 已选择 437 files 将压缩 42.8 MB 已排除 2.38 GB [恢复建议排除] [开始压缩] ``` Tree 应有四个独立状态： ```ts type CheckState = | "checked" | "unchecked" | "mixed" | "excluded"; ``` 其中 `excluded` 和 `unchecked` 最好视觉上分开： ```text unchecked = 用户主动取消 excluded = 规则自动建议排除 ``` 这样用户能知道： > node_modules 为什么默认没选？ 而不是怀疑程序漏文件。 ### 保存策略的渐进增强 最强路径： ```text showSaveFilePicker() ↓ createWritable() ↓ ZipWriter(output) ``` File System Access API picker 要求用户交互，并且权限可能是 `granted / prompt / denied`；持久化 handle 后，下次还应通过 `queryPermission()` / `requestPermission()` 恢复权限，而不能假设永久授权。citeturn12search2turn12search4 第二路径： ```text StreamSaver ``` 适合没有原生 save picker、但支持 Service Worker 流式下载的环境。它的工作机制是 Service Worker 拦截请求并返回一个下载 stream。citeturn7search2 最后才是： ```text Response(stream).blob() → URL.createObjectURL() → <a download> ``` 对于 50 MB 可以接受；对于预估几 GB/几十 GB 的输出，UI 应直接禁止或明显警告。 ## 实施方案与核心代码 ### 工程初始化 建议 TypeScript + React；如果这个工具页面希望非常轻，React 可以直接换 Preact。 ```bash npm create vite@latest local-zip -- --template react-ts cd local-zip npm install npm install @zip.js/zip.js ``` Vite 与 zip.js 配合时，官方文档对 external build 特别提醒：如果使用其外部 worker/WASM entry，开发环境应把 `@zip.js/zip.js` 加到 `optimizeDeps.exclude`，以便 Vite 正确处理 worker/WASM asset。citeturn7search1 例如： ```ts // vite.config.ts import { defineConfig } from "vite"; import react from "@vitejs/plugin-react"; export default defineConfig({ plugins: [react()], optimizeDeps: { exclude: ["@zip.js/zip.js"], }, }); ``` 建议模块： ```text src/ ├─ filesystem/ │ ├─ picker.ts │ ├─ traverse.ts │ └─ fallback.ts │ ├─ tree/ │ ├─ model.ts │ ├─ selection.ts │ └─ excludes.ts │ ├─ compression/ │ ├─ zipjs.ts │ ├─ store.ts │ ├─ scheduler.ts │ └─ progress.ts │ ├─ storage/ │ ├─ native-save.ts │ ├─ streamsaver.ts │ └─ blob-save.ts │ └─ components/ └─ FileTree/ ``` ### 目录权限与递归遍历 Chromium 主路径： ```ts export type TreeNode = { id: string; name: string; path: string; kind: "file" | "directory"; handle: FileSystemFileHandle | FileSystemDirectoryHandle; children?: TreeNode[]; size?: number; }; export async function pickRootDirectory(): Promise<FileSystemDirectoryHandle> { if (!("showDirectoryPicker" in window)) { throw new Error("FILE_SYSTEM_ACCESS_UNAVAILABLE"); } return window.showDirectoryPicker({ mode: "read", id: "zip-source", }); } ``` `showDirectoryPicker()` 要由用户点击事件直接触发，不应该在组件 mount 后偷偷调用，因为该 API 要求 transient user activation。citeturn2search3turn12search4 递归遍历： ```ts export async function walkDirectory( dir: FileSystemDirectoryHandle, parentPath = "", ): Promise<TreeNode[]> { const nodes: TreeNode[] = []; for await (const [name, handle] of dir.entries()) { const path = parentPath ? `${parentPath}/${name}` :
