---
id: "DOCIN-20260825-001-CH-011"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 011"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-011

- Parent intake: `DOCIN-20260825-001`
- Sequence: 11
- Heading: 压缩模式可以比 Bandizip 更聪明
- Page range: null
- Word count: 246
- Hints: `actor-mentioned`, `data-or-integration`

## Text

ZIP 容器实现：zip.js 智能模式： compressionMethod = STORE / DEFLATE 而不是： DEFLATE 用 zip.js STORE 又切 client-zip ``` client-zip 更适合作为性能 benchmark 和应急轻量实现，而不是第一版额外增加一套 archive state machine。 ### 加密选项 zip.js 支持 WinZIP AES 和 ZipCrypto，其中官方自己明确不建议 ZipCrypto，因为它容易被破解；生产 UI 应默认 AES，不要因为“兼容老软件”就默认 ZipCrypto。citeturn6search8 界面可以是： ```text 密码保护 ○ 不加密 ● AES-256 推荐 ○ ZipCrypto 兼容旧软件，但安全性较弱 ``` 另外不要把“加密 ZIP”宣传成“完全隐藏文件结构”。ZIP 的文件名/元数据隐私是另一个问题；client-zip 的维护者也明确指出常见 ZIP 加密方案并不等价于把全部 metadata 隐藏。citeturn7search0 ### 最终项目判断 从这次源码和 API 调研来看，你最初那个想法并不是“浏览器做不了 Bandizip”。 恰恰相反，现在浏览器已经具备了构成它的主要原语： ```text 目录权限 ✓ 完整目录遍历 ✓ webkitdirectory fallback ✓ File.stream() ✓ Web Streams backpressure ✓ Web Worker ✓ Zip64 ✓ AES ZIP ✓ OPFS 临时磁盘 ✓ 直接写用户文件 ✓ Service Worker 流式下载 fallback ✓ ``` 这些能力分别已经由 File System API、Web Streams、OPFS 和 zip.js 等成熟实现覆盖。citeturn2search1turn6search0turn6search2turn7search1turn12search3 真正需要你自己做好的，不是 ZIP 二进制格式，而是三件事： **第一，文件树选择体验。** ```text 父级全选 子级取消 半选状态 搜索 按大小排序 展开/收起 默认排除 批量排除 ``` 这是 Bandizip 当前让你觉得不方便的核心。 **第二，大文件资源调度。** ```text 绝不 arrayBuffer() 全量文件 绝不把大 ZIP 默认生成 Blob 限制 entry 并发 OPFS 承接并行 temp 利用 backpressure 根据文件类型 STORE/DEFLATE ``` zip.js 的当前 API 已经给出了完成这一套的底层组件。citeturn12search3turn13search9 **第三，渐进增强。** ```text Chrome / Edge: showDirectoryPicker + showSaveFilePicker + createWritable + OPFS ↓ API 不可用 Firefox / 其他: webkitdirectory + zip.js + StreamSaver ↓ 仍不可用 小文件: Blob download ``` `browser-fs-access` 的设计本身就是这种 progressive enhancement 思路。citeturn7search3 所以最终优先级我会定为： > **`zip.js + File System Access + Web Streams + OPFS + React/Preact Tree` 是正式架构。StreamSaver/browser-fs-access 是兼容层。fflate 不负责生产大 ZIP 容器；client-zip 作为 STORE 性能参考或可选极速路径。** 而且与直接仿造 Zip Manager 相比，更值得做的是保留它已经验证过的“浏览器本地 ZIP + streaming save”思想，然后重新设计成你真正想要的： ```text 选择整个项目目录 ↓ 可视化分析目录 ↓ 树形自由勾选 ↓ 自动建议排除无用目录 ↓ 实时显示： “将压缩 42 MB，已帮你排除 2.38 GB” ↓ 低内存流式压缩 ↓ 直接写入 ZIP ``` 这不仅技术上成立，而且与现有 Zip Manager、Bandizip 的功能重心都有明显差异。官方 zip.js Create Demo 目前仍然只是“设置密码 → 添加文件 → 查看 ZIP → 下载”的传统流程；这进一步说明，“**压缩前的高质量目录选择器**”依然有足够的产品空间。citeturn11view1
