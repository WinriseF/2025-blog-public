---
id: "DOCIN-20260825-001-CH-006"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 006"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-006

- Parent intake: `DOCIN-20260825-001`
- Sequence: 6
- Heading: 真正应该怎么跑 50 MB / 2 GB / 50 GB
- Page range: null
- Word count: 350
- Hints: `actor-mentioned`, `data-or-integration`

## Text

time output bytes browser process-tree peak RSS average / peak CPU worker count OPFS usage 最终 ZIP size compression ratio ``` 跨浏览器 RAM 最稳妥的比较方法不是只看 `performance.memory`，而是在一个干净 profile 中由外部 harness 统计浏览器根进程及其整个子进程树 RSS；否则 Chromium 的 renderer/utility/worker 多进程模型和 Firefox 进程模型很容易造成“只统计一个 renderer”的错误比较。 最终输出 CSV 建议： ```csv browser,method,dataset,input_gib,output_gib,time_s,throughput_mib_s,peak_rss_mib,avg_cpu_pct,workers,temp_backend,success Chrome,zipjs-opfs,medium,... Edge,zipjs-opfs,medium,... Firefox,zipjs-opfs,medium,... ``` 然后再画真实图；在没有这份 CSV 前，画数值柱状图属于伪造。 不过**内存复杂度**现在就可以确定： ```mermaid flowchart LR A["50 GB 输入"] --> B{"输出方式"} B --> C["Blob 路径"] C --> D["ZIP 数据持续累积"] D --> E["RAM / Blob backing<br/>可能随输出规模增长"] B --> F["直接 WritableStream"] F --> G["有限 chunk + 压缩状态"] G --> H["磁盘"] F --> I{"并行 entries?"} I -->|否| J["低而稳定的工作集"] I -->|是| K["临时 entry buffer"] K --> L["OPFS temp"] L --> H ``` 这里尤其不能简单写成“流式一定恒定 20 MB”。压缩 codec 字典、Worker、浏览器内部 buffer、entry metadata 和 OPFS queue 都需要 RAM；正确说法是**避免输出大小与 JavaScript 堆/Blob 线性绑定**。zip.js 的 `createTempStream` 正是为了把并行 entry 的完整临时数据移出默认内存 TransformStream。citeturn12search3turn12search5 ## 推荐的生产架构 ### 总体数据流 这是我建议真正实施的架构： ```mermaid flowchart TD U["用户点击「选择文件夹」"] --> F{"File System Access?"} F -->|是| D["showDirectoryPicker()"] F -->|否| WD["input[webkitdirectory]"] D --> T["懒遍历 DirectoryHandle"] WD --> RP["webkitRelativePath 重建目录树"] T --> TREE["虚拟化文件树"] RP --> TREE TREE --> CHECK["三态 Checkbox<br/>checked / unchecked / mixed"] CHECK --> RULE["默认排除规则<br/>node_modules .git target dist ..."] RULE --> PLAN["生成 Compression Plan"] PLAN --> M{"文件类型 / 用户模式"} M -->|"文本/源码"| ZIP["zip.js DEFLATE"] M -->|"已压缩媒体"| STORE["STORE / client-zip 可选"] ZIP --> WORKERS["Worker Pool"] STORE --> OUT WORKERS --> TEMP{"并行 entry?"} TEMP -->|否| OUT["最终 ZIP stream"] TEMP -->|是| OPFS["OPFS Temp Streams"] OPFS --> OUT OUT --> SAVE{"showSaveFilePicker?"} SAVE -->|是| CW["createWritable()"] SAVE -->|否且支持 SW| SS["StreamSaver"] SAVE -->|仅小文件| BL["Blob fallback"] CW --> DISK["用户磁盘"] SS --> DISK BL --> DISK ``` ### 为什么不能一上来开很多 Worker zip.js 的 `useWebWorkers` 默认开启，并支持 transfer streams。citeturn13search10 但 CPU 核心数并不意味着应该： ```text hardwareConcurrency = 16 → 同时压 16 个 8GB 文件 ``` 因为 ZIP entry 有顺序约束，而并发压缩意味着后面的 entry 可能先压完，却必须等待前面的 entry 写出；zip.js 因此会启用 buffered write。citeturn13search9 生产建议： ```text 默认并行： min(4, max(1, hardwareConcurrency - 1)) 超大单文件： 并行 entry 数主动降低 OPFS 不可用： 并行压缩建议降到 1~2 小文件极多： 批量调度，避免每个文件单独启动昂贵任务 ``` 这不是 zip.js API 强制值，而是基于其 buffering 模型做出的工程建议。 ### Tree UI 应该成为产品核心能力 Bandizip 让你难受的地方，其实正好可以变成这个网页最大的 UX 卖点： ```text [-] my-project 2.43 GB / 38,421 files ├─ ☑ src 31 MB │ ├─ ☑ components │ ├─ ☑ pages │ └─ ☑ index.ts │ ├─ ☐ node_modules 1.86 GB │ 自动排除 · 31,002 files │ ├─ [-] backend 341
