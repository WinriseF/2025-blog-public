---
id: "DOCIN-20260825-001-CH-003"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 003"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-003

- Parent intake: `DOCIN-20260825-001`
- Sequence: 3
- Heading: 各项目构建方式
- Page range: null
- Word count: 350
- Hints: `actor-mentioned`, `data-or-integration`

## Text

build cd ../fflate npm ci npm run build npm test cd ../client-zip npm ci npm run start npm test ``` ## 项目能力对比与 Demo 源码分析 ### 功能总表 符号说明： `D` = `showDirectoryPicker()`；`WD` = `<input webkitdirectory>`；`FS` = `File/Blob.stream()`；`CW` = `FileSystemFileHandle.createWritable()`；`WS` = Web Streams；`O` = OPFS。 | 项目 | D / WD / FS / CW / WS / O | 真流式 ZIP | Zip64 | 加密 | 并行/Worker | 临时存储 | 主要限制 | |---|---|---|---|---|---|---|---| | **zip.js** | 应用层 / 应用层 / ✓ / 可直连 / ✓ / **✓** | **是**，增量读写 | **✓ 自动** | **AES + ZipCrypto** | **✓ 内建 worker pool** | **OPFS / Sync OPFS / Blob / RAM** | 多 entry 并行会触发 buffered write；应配置 temp stream | | **zip.js create demo** | 未使用 D / 未见 WD / 间接 / Blob 下载 / 库支持 / 未配置 | 核心库支持，但 demo 最终是下载式 UI | ✓ | ✓ 密码输入 | 继承库 | demo 未显式配置 | 示例不是大目录生产 UI | | **zip-manager** | ✓ 输出目录 / 无 WD 源目录逻辑 / 间接 / **✓** / ✓ / 未显式配置 | **现代保存路径是** | 继承 zip.js | 继承 zip.js | 继承 zip.js | 当前源码未见 OPFS temp 配置 | fallback 最终会生成 Blob；没有你要的 checkbox tree | | **StreamSaver.js** | — / — / 可接输入流 / 不调用原生 CW / **✓** / — | **是** | N/A | N/A | 无压缩 worker pool | Service Worker 管道 | SW 生命周期、HTTPS、Firefox/浏览器行为 | | **browser-fs-access** | **✓** / **✓ fallback** / ✓ / **✓ modern** / ✓ / — | 保存 Response 可流式 | N/A | N/A | N/A | N/A | legacy save 最终仍受浏览器下载机制约束 | | **fflate** | 应用层 / 应用层 / 需适配 / — / 自己的 chunk API / — | **是** | **写入不完整** | **无 ZIP 加密** | **✓ Web Worker** | 应用自行处理 | 大 ZIP/大单文件写 Zip64 不适合生产需求 | | **client-zip** | 应用层 / 应用层 / **✓ ReadableStream** / 应用层 / **✓** / — | **是** | **✓** | **无** | 无压缩多核需求 | 不需要全量缓存 | **不压缩**；CRC-32 是主要 CPU 瓶颈 | zip.js 官方当前明确列出 Zip64、WinZIP AES、ZipCrypto、同时读写、worker pool 和可自定义 Worker/压缩引擎；其默认 `useWebWorkers` 和 `transferStreams` 也都是开启状态。citeturn7search1turn13search10 更关键的是，ZipWriter 的 `zip64` 会在必要时自动启用，例如压缩数据超过 4 GB 或输入大小未知；所以不能把 zip.js 理解成“只能普通 ZIP”。citeturn6search8turn14search1 ###
