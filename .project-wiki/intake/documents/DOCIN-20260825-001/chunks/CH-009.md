---
id: "DOCIN-20260825-001-CH-009"
type: intake-chunk
status: active
title: "Browser ZIP Compression Deep Research - Chunk 009"
created: "2026-08-25T15:48:12+08:00"
updated: "2026-08-25T15:48:12+08:00"
tags: [intake, chunk]
related: ["DOCIN-20260825-001"]
source_paths: ["C:/Users/Flynn/Desktop/deep-research-report.md"]
confidence: confirmed
---

# DOCIN-20260825-001-CH-009

- Parent intake: `DOCIN-20260825-001`
- Sequence: 9
- Heading: zip.js + WritableStream + OPFS
- Page range: null
- Word count: 350
- Hints: `requirement-language`, `security-or-compliance`, `data-or-integration`

## Text

// 不要毫无限制 Promise.all 10 万个文件。 const pool = new TaskPool(4); await Promise.all( files.map((item) => pool.run(async () => { if (signal.aborted) { throw new DOMException("Cancelled", "AbortError"); } const file = await item.handle.getFile(); let previous = 0; await writer.add( item.path, file.stream(), { uncompressedSize: file.size, signal, onprogress(current) { totalDone += current - previous; previous = current; onProgress(totalDone, totalBytes); }, }, ); }), ), ); await writer.close(); } catch (error) { try { await output.abort(error); } catch { // 避免二次错误覆盖真正的压缩错误。 } throw error; } } ``` zip.js 官方 API 确认 `ZipWriter.add` 支持 `signal`、`onprogress`、Zip64、worker 配置和 `createTempStream`；`File/Blob.stream()` 则产生标准 ReadableStream。citeturn14search1turn6search2 这里尤其不要做： ```ts const buffers = await Promise.all( files.map((file) => file.arrayBuffer()) ); ``` 因为那才是真的： ```text 所有输入 → RAM ``` 也不要最终： ```ts const blob = await new Response(zipStream).blob(); ``` 然后把它当成 50 GB 主路径。 ### Worker pool 不要等于无限 Promise.all 简单调度器： ```ts class TaskPool { private active = 0; private readonly queue: Array<() => void> = []; constructor(private readonly limit: number) { if (limit < 1) { throw new Error("Pool limit must be >= 1"); } } async run<T>(task: () => Promise<T>): Promise<T> { await this.acquire(); try { return await task(); } finally { this.release(); } } private acquire(): Promise<void> { if (this.active < this.limit) { this.active += 1; return Promise.resolve(); } return new Promise((resolve) => { this.queue.push(() => { this.active += 1; resolve(); }); }); } private release(): void { this.active -= 1; this.queue.shift()?.(); } } ``` 实际上 zip.js 自己已有 worker pool，因此这个 pool 管的是： > **同时进行多少个 ZIP entry 任务** 而不是自己再次实现 Deflate Worker。 zip.js 官方 worker 配置本身默认启用 worker，而且 transfer streams 默认也是 true。citeturn13search10 ### 取消、错误和“恢复” 取消非常适合用 `AbortController`： ```ts const controller = new AbortController(); function cancelCompression() { controller.abort( new DOMException("用户取消压缩", "AbortError"), ); } ``` zip.js 当前 API 原生支持 `AbortSignal`。citeturn14search1 错误建议分层： ```ts type ZipErrorCode = | "SOURCE_PERMISSION_DENIED" | "SOURCE_CHANGED" | "DESTINATION_PERMISSION_DENIED" | "DISK_FULL" | "OPFS_QUOTA_EXCEEDED" | "ZIP_FAILED" | "USER_CANCELLED"; ``` UI 不应该统一显示： ```text 压缩失败 ``` 而应例如： ```text 目标磁盘空间不足 预计 ZIP：28.4 GB 当前可用空间不足以继续写入。 ``` “断点续压”需要特别谨慎。 **不要在 V1 宣传成类似下载器的真正断点续传。** ZIP 最终还需要 central directory；浏览器退出后，Worker、ZipWriter 状态、CRC/offset/entry order 都需要恢复。把“已经写了 37 GB 的半成品 ZIP”安全接着写，不只是记一个文件序号这么简单。 V1 更稳妥的是： ```text 持久化： 选择规则 排除规则 压缩等级 源目录 handle 目标 handle 任务清单 取消/崩溃： 删除/abort 临时输出 重新压缩 ``` File System API handles 可以序列化到 IndexedDB；但重新使用时权限可能回到 `prompt`，所以要再次做 permission check。citeturn2search1turn12search2
